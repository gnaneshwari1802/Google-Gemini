```
1.cls
2.conda create -p venv python==3.10 -y
3.conda activate "D:\Complete-Langchain\Blog Generation\venv" or conda activate venv/
4.pip install -r requirements.txt
5.streamlit run app.py
6.conda install -c conda-forge poppler
7.pip install pdf2image
8. pip install -U langchain-community  
9.install poppler-utils

```

```
The Llama 2 release introduces a family of pretrained and fine-tuned LLMs, ranging in scale from 7B to 70B parameters (7B, 13B, 70B). The pretrained models come with significant improvements over the Llama 1 models, including being trained on 40% more tokens, having a much longer context length (4k tokens 🤯), and using grouped-query attention for fast inference of the 70B model🔥!

```

```
1.git init
2.git commit -m "Remove venv from tracking"
3.git rm -r --cached venv
4.# .gitignore
5.git add .
6.git commit -m "first commit"
7.git branch -M main
8.git remote add origin https://github.com/gnaneshwari1802/Complete-Langchain.git
9.git push -u origin main
10.git remote remove origin
11.git filter-branch  
12.git reset HEAD--venv
13.git add README.md
14.git status

```
```
https://aistudio.google.com/app/apikey

```
